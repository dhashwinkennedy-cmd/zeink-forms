
/**
 * Redundant storage service neutralized to prevent Firebase initialization conflicts.
 * Use services/firebase.ts for all storage and auth logic.
 */
export const isLive = false;
export const storage = {};
